{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "A very awesome Hypixel SkyBlock texture pack remover.",
  "authors": [
    "Starred"
  ],
  "contact": {},
  "license": "BSD 3-Clause",
  "icon": "assets/detexturify/icon.png",
  "environment": "*",
  "entrypoints": {
    "client": [
      {
        "value": "foo.starred.detexturify.Detexturify",
        "adapter": "kotlin"
      }
    ]
  },
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.18",
    "fabric-api": "*",
    "fabric-language-kotlin": ">=${kotlin}",
    "minecraft": "${minecraft}"
  },
  "accessWidener": "${accessWidener}"
}